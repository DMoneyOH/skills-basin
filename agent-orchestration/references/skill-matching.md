# Skill Matching
## Overview
The skill matching process matches the user's request with the available skills in the registry.
## How It Works
1. The `match_skills.py` script is executed with the user's request as input.
2. The script matches the request with the skills in the registry based on their capabilities.
3. The script returns a JSON response with the matched skills, ranked by relevance.