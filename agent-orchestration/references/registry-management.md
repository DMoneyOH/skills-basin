# Registry Management
## Overview
The registry management process manages the registry of available skills.
## How It Works
1. The registry is stored in `agent-orchestrator/data/registry.json`.
2. The registry is updated by the auto-discovery process.
3. The registry is used by the skill matching and orchestration processes to determine the available skills.